{% extends 'layout.html.twig' %}

{% block title %}
    Stras'Help – {{ article.title}}
{% endblock %}

{% block content %}
<h2 class="col-12 text-center my-4">{{ article.title}}</h2>
    <div class="container row col-12 m-0">
                <div class="card col-12 my-1 border-black">
                <div class="blogView" style="background-image:url('{{ article.image}}')"></div>
                <h3>{{ article.category }}</h3>
                    <div class="card-body">
                        <div class="column justify-content-between">
                            <div>
                                <h1>{{article.title}}</h1><br/>
                                <i>{{article.category | capitalize}}</i>
                            </div>
                            <div class="text-left">
                                {{ article.date_publication|date("d/m") }}
                            </div>
                        </div>
                        <div class="text-justify">
                            <p>{{ article.bodytext | raw }}</p>
                        </div>
                    </div>
                </div>
        </div>
    <div>&nbsp;</div>
{% endblock %}